package StateOfThread;
/*
NEW(新建) 至今尚未启动的线程处于这种状态。
RUNNABLE 正在Java虚拟机中执行的线程处于这种状态。
BLOCKED  受阻塞并等待某个监视器锁的线程处于这种状态。
WAITING  无限制地等待另一个线程来执行某一特定的线程处于这种状态。
TIMED WAITING  等待另一个线程来执行某一特定操作的线程处于这种状态
TERMINATED     已退出的线程处于这种状态
 */
public class Defination {
}
